# Core
# Streamlit va FIJADO a propósito. Con `>=` cada redespliegue de Streamlit Cloud
# puede traer otra versión, y el renderizado de las tablas cambia entre ellas:
# el 31-jul-2026 la app desplegada escribía «None» en las celdas nulas mientras
# que en local (1.59.1) salían vacías, con el mismo código y los mismos datos.
# Un dashboard que se ve distinto según el día que redesplegó no es depurable.
streamlit==1.59.1
# pandas, numpy y pyarrow también fijados, y por el mismo motivo: entre pandas 2
# y 3 cambia cómo se convierten los nulos al serializar a Arrow, que es
# exactamente lo que decide si una celda vacía se ve vacía o se ve «None».
# Con `>=` el servidor instalaba pandas 2.x mientras en local había 3.0.3, así
# que el mismo código y los mismos datos daban pantallas distintas y no había
# forma de reproducir el fallo.
pandas==3.0.3
numpy==2.5.1
pyarrow==24.0.0
plotly>=5.20
requests>=2.31

# Conectores de API (opcionales — solo si usas datos en vivo)
google-ads>=25.0            # Google Ads API
google-analytics-data>=0.18 # GA4 Data API
google-auth>=2.29           # Service Account para GA4
google-api-python-client>=2.120  # YouTube Data API v3 + YouTube Analytics v2
google-auth-oauthlib>=1.2   # OAuth de usuario para YouTube (no admite SA)
# HubSpot, Meta (Ads y orgánico) y LinkedIn usan REST directo vía `requests`
